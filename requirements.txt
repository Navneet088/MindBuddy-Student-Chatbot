# ─────────────────────────────────────────────
# MindBuddy — Requirements
# Python 3.9+
# ─────────────────────────────────────────────

# UI Framework
streamlit>=1.32.0

# LangChain Core + Groq Integration
langchain>=0.2.0
langchain-core>=0.2.0
langchain-groq>=0.1.6

# Groq Python SDK (used internally by langchain-groq)
groq>=0.9.0

# HuggingFace Transformers (BERT emotion model)
transformers>=4.40.0

# ML Runtime (required by transformers pipeline)
torch>=2.0.0

# Tokenizer backend (required by BERT tokenizer)
tokenizers>=0.19.0

# Numerical computing (transformers dependency)
numpy>=1.24.0
